machine:
  node:
    version: 7.4.0

dependencies:
  override:
  curl -sL https://deb.nodesource.com/setup_7.x | sudo -E bash -
  sudo apt-get install -y nodejs
test:
  nvm ls
  post:
    chmod + x ./dbcheck2.js
    node ./dbcheck2.js
